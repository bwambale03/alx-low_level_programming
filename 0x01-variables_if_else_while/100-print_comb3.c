#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - Entry
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a, b = q;
	a = 1;
	b = 0;

	for (a = '1' && b = '0')
	{
		putchar(q);

		if (%d = q)
		{
			putchar(',');
			putchar(' ');
		}
		putchar('\n');

		return (0);
	}
}
